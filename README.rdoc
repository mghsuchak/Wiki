This is a Wikipedia Type Application(built in Ruby on Rails). It has the ability to Add, Edit, Destroy articles postings and each article is assigned to a category, and also one can filter between the various categories to see only articles within that category (For example, only “Art” or only “Technology” articles).
